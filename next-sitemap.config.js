/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://www.spendelonmuskmoney.org/', // Replace with your site's URL
    generateRobotsTxt: true, // (Optional) Generate `robots.txt` file
    sitemapSize: 5000, // Limit sitemap size (optional)
  };
  